# signalsbot Rust Example

Minimal router-intent listener for Grexie Signals. It subscribes through `SignalsManager`, logs server-created intents, and leaves venue execution to your own integration.

```sh
cp .env.example .env
cargo run
```
